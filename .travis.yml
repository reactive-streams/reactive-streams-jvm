language: java
script:
  - ./gradlew check --info
cache:
  directories:
  - $HOME/.gradle
jdk:
  - openjdk6
env:
  global:
  - TERM=dumb
  - DEFAULT_TIMEOUT_MILLIS=300
  - PUBLISHER_REFERENCE_GC_TIMEOUT_MILLIS=300
